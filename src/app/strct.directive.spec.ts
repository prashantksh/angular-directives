import { StrctDirective } from './strct.directive';

describe('StrctDirective', () => {
  it('should create an instance', () => {
    const directive = new StrctDirective();
    expect(directive).toBeTruthy();
  });
});
